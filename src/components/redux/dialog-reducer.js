
let initionalState = {
  qwerty: [
    { id: 888, name: ')))' },
    { id: 999, name: ')))' },
  ]
};


let dialogReducer = (state = initionalState, action) => {

  return state;
};

export default dialogReducer;